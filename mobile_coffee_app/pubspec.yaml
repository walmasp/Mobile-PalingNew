name: mobile_coffee_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  provider: ^6.1.1
  http: ^1.6.0
  local_auth: ^3.0.1
  sensors_plus: ^5.0.1
  shared_preferences: ^2.5.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  proximity_sensor: ^1.3.10
  light: ^5.0.0
  geolocator: ^14.0.2
  flutter_local_notifications: ^21.0.0
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
